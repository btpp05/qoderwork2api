services:
  qw2api:
    build: .
    container_name: qoderwork2api
    restart: unless-stopped
    ports:
      - "7864:7864"
    volumes:
      - ./auths:/app/auths
      - ./data:/app/data
      - ./config.json:/app/config.json:ro
